{
	"name": "@synpatico/express-middleware-example",
	"version": "1.0.0",
	"type": "module",
	"description": "Production-ready Express middleware example for Synpatico",
	"scripts": {
		"start": "concurrently \"node server.js\" \"cd react-demo && npm run dev\"",
		"dev": "concurrently \"node --watch server.js\" \"cd react-demo && npm run dev\"",
		"server": "node server.js",
		"react": "cd react-demo && npm run dev"
	},
	"dependencies": {
		"@synpatico/core": "workspace:*",
		"@synpatico/genome": "workspace:*",
		"express": "^4.19.2"
	},
	"devDependencies": {
		"concurrently": "^8.2.2"
	}
}
